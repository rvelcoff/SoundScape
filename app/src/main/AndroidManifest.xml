<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soundscape">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoundScape">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimalsActivity"/>
        <activity android:name=".MedievalActivity"/>
        <activity android:name=".MusicActivity"/>
        <activity android:name=".NatureActivity"/>
        <activity android:name=".SpaceActivity"/>
        <activity android:name=".YourOwnActivity"/>
    </application>

</manifest>